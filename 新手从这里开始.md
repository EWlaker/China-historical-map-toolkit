# 新手从这里开始

这份文档假设你**从没用过 Python，也不知道什么是命令行**。

照着做就行。全程大概十五分钟。

---

## 这个工具能帮你做什么

你有一堆老地图的扫描件，一张一个文件。它们现在大概是这样：

- 每一张都歪着一点
- 四周是黑边和一堆空白纸边
- 大小不一
- 放进 GIS 里，软件不知道它该待在哪

这个工具会自动帮你做三件事：把每张图摆正、裁掉多余边距、统一尺寸；算出每张图该在的经纬度，写成 GIS 认识的格式；再生成一个索引文件，拖进 QGIS 就能看到全部地图拼在一起的样子。

---

## 第一步：装 Python

已经装过的可以跳过。不确定装没装，按 `Win + R`，输 `cmd` 回车，在黑窗口里敲：

```
python --version
```

出来 `Python 3.x.x` 就是有了。

没有的话：

去 https://www.python.org/downloads/ 下载。

双击安装程序，**安装界面最下面有个勾选框，写着 `Add Python to PATH`，必须勾上。**

不勾的后果是：Python 装上了，但你在命令行敲 `python` 它说找不到。这个坑我踩过，也见过太多人踩。

勾完点 Install Now，等它装完。

---

## 第二步：先拿示例跑一遍

**别一上来就放自己的图。** 先用自带的示例走一遍，确认环境没问题。

把工具包解压到一个**路径里没有中文和空格**的地方，比如 `D:\map-toolkit\`。

（桌面其实也能跑，但桌面路径通常带中文，有些库遇到中文路径会出问题。这个我处理过了，但少一个变量总是好的。）

进那个文件夹，双击两个东西。

先双击 `生成示例数据.bat`，它会造 6 张假的测试地图——不是真地图，是程序画的、带棋盘格和噪声的图，只用来验证流程。

再双击 `一键运行.bat`。

接下来黑窗口会自己滚动。它会问你三件事：

```
============================================================
  开始之前，先确认三件事（每题直接回车 = 用括号里的默认值）
============================================================

  [1/3] 输出色彩
     1 = 自动判断      黑白图出灰度、有颜色的图保住颜色   【默认，推荐】
     2 = 强制灰度      不管原图什么颜色，一律转成黑白
     3 = 强制彩色      一律保留颜色

  [2/3] 输出尺寸
     1 = 4500x3500     统一尺寸，配经纬度网格用          【默认】
     2 = 保持原始像素  不缩放，各幅尺寸可能不一致
     3 = 自己填        例如 3000x2000

  [3/3] 这批图的文件名里有标准图号吗？
     1 = 有   形如 2108-瑪瑙觀.jpg                       【默认】
     2 = 没有 我要做地理配准，但文件名不是这个格式
     3 = 没有 我只要裁切，不需要经纬度
```

**拿不准就一路回车**，三题的默认值是最常用的组合。

第 1 题：民国地形图基本都是黑白的，选 1 它自己会判断成灰度。手上是交通图、水系图那种带颜色的，选 1 也能认出来，不用手动改成 3。

第 2 题：4500×3500 是照着五万分一图幅的比例定的。**你的图幅比例跟这个差很多的话，选 1 会轻微拉变形。** 拿不准可以先测一下：

```
python scripts\tools\probe_resolution.py
```

它会抽样测出你的图裁完之后实际是多少像素，给你几档建议，还会告诉你比例差异大不大。

第 3 题最关键，决定后面跑不跑配准。

跑完会自动弹出 `work` 文件夹。里面三样东西：

`work\01_crop\` 是裁好的图，普通 PNG，双击就能看。

`work\02_geo\` 是配准好的 GeoTIFF，需要 GIS 软件才看得了，QGIS 就行，免费的。

`work\02_geo\mosaic.vrt` 是索引文件，直接拖进 QGIS 窗口，能看到所有图拼在一起。

打开 `work\01_crop\` 看一眼。示例原本是歪的、带黑边的，跑完应该变得方方正正、尺寸一致。这样就成了。

示例数据试完可以直接删，不影响后面。

---

## 第三步：换成你自己的图

### 把文件放进 input

全选复制到 `input\` 文件夹里。示例文件（`2108-甲幅.jpg` 这种）先删掉。

### 改名

**如果你要做配准，这一步必须要做**，因为工具是靠文件名判断每张图该在哪的。

格式是四位数字加短横线加图名：

```
2108-瑪瑙觀.jpg
0119-花凉亭.jpg
```

前两位是列号，从右往左数；后两位是行号，从上往下数。`2108` 就是第 21 列、第 8 行。

这四位数字从哪来？从接图表上来，或者看原图图廓上印的图号。

**不要自己猜。** 这话得说重一点——编号错一位，整幅图就挪到隔壁去了，而且**它不会报错**。跑出来的结果看着完全正常，只是位置错了。

我这次就遇到两幅编号比实际大 1 的，是拿接图表逐格核对才发现的。

### 核对图号规则

工具需要知道"第 21 列"对应哪个经度。这个换算写在 `config.yaml` 里，默认值是按湖北那套图定的，**你的图集未必一样**。

核对方法：随便挑一幅原图打开，看图廓右上角印的经纬度，比如 `111°15'` 和 `32°10'`。

然后拿记事本打开 `config.yaml`，找到 `grid:` 那一段，里面有四个数。按这个公式算：

```
西边界经度 = 116.25 − 0.25 × 列号
北边界纬度 = 33.333333 − 行号 ÷ 6 + 0.166667
```

拿 `2108` 举例，列号 21、行号 8：

西边界 = 116.25 − 0.25 × 21 = 111.00°
北边界 = 33.333333 − 8÷6 + 0.166667 = 32.1667°

32.1667° 换成分就是 32°10'，跟图廓上印的对得上。

经度这边，111.00° 是图的西边界，那东边界应该是 111.00 + 0.25 = 111.25°，也就是 111°15'，也对得上。

**两个都对上，说明规则没错，可以开跑。对不上就别跑**——公式里那四个数得改，猜错了整批数据都会错位。

（这是整套流程里最容易出问题的地方。真对不上，把"某一幅的图号 + 它图廓上印的经纬度"拿出来，改那四个数就是解方程，不难，只是得先确认。）

### 把非地图文件排除掉

`input` 里只放地图。

接图表、封面、说明页这些不要放进去，或者放进去了要在配置里排除。否则会被当成普通图幅处理，白多出几个文件。

在 `config.yaml` 里加一行：

```yaml
crop:
  exclude: ["index", "封面"]
```

写文件名，不含扩展名。

### 跑

双击 `一键运行.bat`，回答那三题，然后等。

**图幅多的话会比较久。** 裁切阶段平均每幅三到五秒，400 幅大概二三十分钟。中间别关窗口。

**建议先只放一幅试跑。** 参数没调好就跑几百幅会白等很久——这个我吃过亏。

---

## 第四步：看结果

跑完弹出的 `work` 文件夹。

**想看裁得对不对**，去 `work\01_crop\`。普通图片，双击就能看。重点看三件事：

图正不正——原本歪的应该被摆正了。

边裁掉没有——黑边和多余的纸边应该没了。

**内容有没有被切掉**——这个最要紧。工具的设计原则是"宁可留白，绝不切内容"，因为裁多了不可逆，留白只是不好看。但万一还是切了，看下面第五节第 ③ 条。

**想看整体**，装个 QGIS（免费的），把 `work\02_geo\mosaic.vrt` 拖进去。所有图会按正确位置拼在一起。

---

## 出错了怎么办

### ① 双击 .bat 一闪而过，或者提示"没有找到 Python"

Python 没装，或者装了但没勾 `Add Python to PATH`。

重新跑一遍安装程序，选 Modify，把那个勾选框补上。实在不行卸载重装，安装第一屏记得勾。

### ② 提示缺依赖库，然后装失败

多半是网的问题。在文件夹空白处按住 Shift 点右键，选"在此处打开 PowerShell"，敲：

```
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

后面那段是清华镜像，国内下得快很多。

### ③ 发现某幅图的内容被切掉了一块

这是最需要当回事的问题。

稳妥的办法是在 `config.yaml` 的 `force_safe` 里加上那一幅：

```yaml
crop:
  force_safe: ["0217-桐山", "1506-湖陽鎮"]
```

重新跑，这两幅会走"整幅保留"的路径——边缘可能留白，但绝对不会切内容。

**但我更想说另一个办法：直接自己手动裁。**

如果只有一两幅有问题，用 Photoshop 或者随便什么工具手动裁一下，比调参数快得多。工具是批量的，批量的代价就是它不可能对每一幅都最优。

顺便，把出问题的文件名记下来。这类样本对改进工具有用。

### ④ 大部分图都留白很多

`config.yaml` 里找 `density_threshold: 0.10`，改成 `0.12` 或 `0.13`，然后**先只放一幅**试跑看效果。

如果改完反而开始切内容了，就退回去，或者更低一点（`0.08`）。

### ⑤ 提示"文件名不符合规则，已跳过"

文件名格式不对。检查是不是四位数字开头、紧跟着一个短横线：

```
✗ 瑪瑙觀.jpg          没有数字
✗ 2108瑪瑙觀.jpg      数字后面少了横线
✗ 21-08-瑪瑙觀.jpg    列和行分开了
✓ 2108-瑪瑙觀.jpg
```

### ⑥ 图在 GIS 里整体偏了

这个不是 bug，是老地图本身的坐标系跟现在用的有出入。

在 `config.yaml` 里找 `east_shift_m: 0.0`，改成偏移的米数，向东为正。

湖北这套实测下来偏移在 5 到 10 公里之间。我最后是整体往东移了一个距离，让它跟天地图影像对得上。

**代价要说清楚**：移完之后，图幅自带的经纬度注记就跟实际位置不一致了。

所以这是个取舍。想保留原图的坐标体系就别动；想跟现代底图叠着用，就得移。这个数是我在湖北这套上量出来的，**换别的图集你要自己量**。

---

## 没有图号怎么办

这种情况很常见：手上是一套连续的分幅图，但文件名是档案编号（比如 `F_004028`），不是图号，也查不到接图表。

**这种图做不了地理配准**——算不出经纬度。但工具还是有用的。

**办法一：只裁切。**

在上面第 3 题选 3。工具裁完就停，不去跑配准。

（以前这一步会直接报错退出，前面的裁切白跑一遍。现在会干净地跳过。）

**办法二：手工拼出位置关系。**

如果这批图本来是一整张切开、彼此相邻的，双击 `拼图工作台.bat`。

浏览器会自动打开一个工作台。左边是缩略图，中间是画布。

先点右上角"自动排布"，所有图会按顺序铺开。然后像拼拼图一样把图拖到正确位置——看省界、河流、道路能不能接上。

滚轮缩放，空白处按住能拖动。拖错了双击退回。拖到已经有图的格子上，两幅会对调位置。

摆好之后点"一键生成拼图"，选清晰度（可以多选，一次出好几个文件），点开始，它自己拼完并打开成果文件夹。

（这个功能用本地服务实现，所以那个黑窗口要一直开着，拼完再关。）

**得到什么、得不到什么，说清楚：**

能拼出一张真正连在一起的整图，颜色也保得住。**但它是没有地理坐标的**，不能跟卫星影像叠加，也不能量距离面积。相邻图幅之间靠肉眼判断，接缝处可能有错位。

判断"是不是一套连续的图"很简单：看几幅缩略图，相邻两幅的边缘能接上（同一条河、同一条路继续延伸）就是一套。

---

## 名词速查

| 词 | 意思 |
|---|---|
| **图廓** | 地图四周的边框线，框内才是地图内容 |
| **图幅** | 一幅地图，也就是一个扫描文件 |
| **裁切** | 把图廓外面的黑边、空白、标题都裁掉 |
| **配准** | 告诉电脑"这张图应该在地球的这个位置" |
| **经纬度** | 表示地球位置的坐标，比如东经 111°、北纬 32° |
| **GIS** | 地理信息系统，能在地图上叠加数据的软件，如 QGIS、ArcGIS |
| **GeoTIFF** | 带位置信息的图片文件，GIS 能自动认到正确位置 |
| **VRT** | 一个索引文件，本身不是图片，但能指向一堆图片并拼在一起 |
| **接图表** | 一张总图，上面画着所有图幅的格子，标着每格的图号 |
| **金字塔** | 图片的多个缩小版本，让 GIS 缩放时更快 |
| **拼图工作台** | 一个网页工具，用拖拽的方式手工确定图幅之间的位置关系 |

---

## 文件夹里都是什么

```
historical-map-toolkit\
├── 一键运行.bat          双击这个就跑（最常用）
├── 自定义尺寸裁切.bat     只裁切，运行时可自己填宽高
├── 拼图工作台.bat         没有图号时，拖拽确定位置关系
├── 拼接成果.bat           把拼图结果拼成一张整图
├── 生成示例数据.bat       想先试效果时双击这个
├── 新手从这里开始.md      你正在看的文件
├── README.md             更详细的技术说明
├── config.yaml           配置文件，用记事本打开改
├── input\                把你的地图放这里
├── work\                 跑完的成果在这里
├── docs\                 原理和参数说明（进阶阅读）
└── scripts\              程序本体，不用动
```

平时你只会用到 `一键运行.bat` 和 `input` 文件夹。遇到特殊情况——只想裁切、或者手上没有图号——再用另外那几个。
