# 新手从这里开始

这份文档假定你**从没用过 Python，也不知道什么是命令行**。
照着做就行。全程大约 15 分钟。

---

## 这个工具能帮你做什么

你有一堆**老地图的扫描件**（一张一个文件，jpg 或 png）。
它们现在大概是这样的：

- 每一张都歪着一点
- 四周有黑边、有大量空白纸边
- 大小不一样
- 放进 GIS 软件里不知道它该在哪个位置

这个工具会**自动**帮你：

1. 把每张图摆正、裁掉多余边距、统一尺寸
2. 算出每张图该在的经纬度位置，写成 GIS 能认的格式
3. 生成一个索引文件，拖进 QGIS 就能看到全部地图拼在一起的样子

---

## 第一步：装 Python

> 已经装过 Python 的可以跳到第二步。
> 不确定装没装：按 `Win + R`，输入 `cmd` 回车，在黑窗口里输入 `python --version`。
> 如果显示出 `Python 3.x.x` 就是装好了。

1. 打开 <https://www.python.org/downloads/>
2. 点黄色的大按钮 **Download Python 3.x.x**
3. 双击下载好的安装程序
4. **⚠ 最关键的一步**：安装界面**最下面**有个勾选框
   **`Add Python to PATH`**（或 `Add python.exe to PATH`），
   **一定要勾上！** 不勾的话后面全都会失败。
5. 点 **Install Now**，等它装完

---

## 第二步：先拿示例试一遍

**别急着放自己的数据。** 先用自带的示例跑一遍，确认环境没问题。

1. 把 `historical-map-toolkit.zip` 解压到一个**路径里没有中文和空格**的地方，
   比如 `D:\map-toolkit\`
   （放桌面也行，但桌面路径通常带中文，有时会出问题）
2. 进入解压出来的文件夹
3. **双击 `生成示例数据.bat`** —— 它会生成 6 张假的测试地图
4. **双击 `一键运行.bat`**

接下来你会看到一个黑色窗口自己滚动，依次显示：

```
[1/5] 检查 Python ...        ✓
[2/5] 检查依赖库 ...         第一次会自动安装，需要联网等 1-3 分钟
[3/5] 检查输入目录 input\ ... 找到 6 个图片文件
[4/5] 开始处理 ...
      第 1 关 / 共 3 关：裁切 ...
      第 2 关 / 共 3 关：配准 ...
      第 3 关 / 共 3 关：生成拼接索引 ...
[5/5] 自查 ...               全部检查通过
```

最后会自动弹出 `work` 文件夹。里面：

| 文件夹 | 里面是什么 | 怎么看 |
|---|---|---|
| `work\01_crop\` | 裁好的图 | 普通图片，双击就能看 |
| `work\02_geo\` | 带坐标的图 | 需要 GIS 软件（如 QGIS） |
| `work\02_geo\mosaic.vrt` | 拼接索引 | 用 QGIS 打开 |

**打开 `work\01_crop\` 里的图看看**——如果原本歪的、带黑边的图
都变得方方正正、尺寸一致，那就成功了。

> 示例数据是程序生成的**假地图**，只用来验证流程。试完可以直接删掉，
> 或者留着，不影响后面。

---

## 第三步：换成你自己的地图

### 3.1 把文件放进 input 文件夹

把扫描件全部复制到 `input\` 文件夹里。
**先把示例文件删掉**（`2108-甲幅.jpg` 这类名字的）。

### 3.2 给文件改名

这是**必须做的一步**。工具靠文件名来判断每张图该放在哪。

要求的格式是：**`四位数字-图名.jpg`**

```
2108-瑪瑙觀.jpg
0119-花凉亭.jpg
3023-丁砦.jpg
```

那四位数字是什么意思？

- **前两位是"列号"**，从右往左数
- **后两位是"行号"**，从上往下数

比如 `2108` 就是"第 21 列、第 8 行"。

> **这四位数字从哪来？**
> 从地图的**图集接图表**（一张标着所有图幅编号的总图）上来，
> 或者看原图图廓（地图边框）上印的图号。
> **不要自己猜。**

### 3.3 ⚠ 核对图号规则（最重要的一步）

工具需要知道"第 21 列"对应哪个经度。这个换算关系写在配置文件里，
**默认值不一定适合你的图集**，所以必须先核对。

**怎么核对：**

1. 随便挑一幅原图打开
2. 看它**图廓（边框）右上角**印的经纬度，比如写着 `111°15'` 和 `32°10'`
3. 用记事本打开 `config.yaml`，找到 `grid:` 这一段：

```yaml
grid:
  rule: CCRR
  ccrr:
    origin_lon: 116.25
    step_lon:   0.25
    origin_lat: 33.333333333
    step_lat:   0.166666667
```

4. 按这个公式算一下：

   **西边界经度 = 116.25 − 0.25 × 列号**
   **北边界纬度 = 33.333333333 − 行号 ÷ 6 + 0.166666667**

5. 举例：图号是 `2108`，列号 21、行号 8

   - 西边界 = 116.25 − 0.25 × 21 = **111.00°**
   - 北边界 = 33.333333 − 8 ÷ 6 + 0.166667 = **32.1667°**

   32.1667° 换算成度分就是 **32°10'**

   图廓上印的是 `111°15'` 和 `32°10'`？
   - 纬度对上了 ✓
   - 经度 111.00° 是**西**边界，图廓**东**边界应该是 111.00 + 0.25 = 111.25° = **111°15'** ✓

   **两个都对得上，说明规则正确，可以开工。**

**如果对不上**，说明你的图集用的是别的编号体系或别的网格尺寸。
这时候请把"某一幅的图号 + 它图廓上印的经纬度"发给技术人员，
让他帮你改配置——**不要自己硬试**，猜错了整批数据都会错位。

### 3.4 把非地图文件排除掉

`input` 文件夹里**只放地图**。如果你同时把**接图表、封面、说明页**也放进去了，
必须在配置里排除它们，否则它们会被当成地图处理，白白多出几个文件。

用记事本打开 `config.yaml`，找到 `exclude`：

```yaml
crop:
  exclude: ["index", "封面", "说明"]
```

把那些文件的**文件名（不含扩展名）**写进去。

### 3.5 运行

双击 **`一键运行.bat`**，等它跑完。

**图幅多的话（比如几百幅）会比较久**，裁切阶段大约每幅 3~5 秒，
400 幅大概 20~30 分钟。中途别关窗口。

> **强烈建议先试跑一幅。**
> 把其他文件先挪走，只留一幅在 `input` 里，跑一遍看看效果对不对，
> 再放全部进去。参数没调好就跑几百幅，会白等很久。

---

## 第四步：看结果

跑完后弹出 `work` 文件夹：

### 4.1 想直接看图 → `work\01_crop\`

普通 PNG 图片，双击就能看。检查这几件事：

- 图是不是**方正**了（原本歪的应该被摆正）
- 黑边和多余空白是不是**被裁掉了**
- 地图内容有没有**被切掉一块**（这是最要紧的！）

**如果发现某幅图被切掉了内容**，看下面"出错了怎么办"第 3 条。

### 4.2 想在 GIS 里看整体 → `work\02_geo\mosaic.vrt`

1. 下载安装 [QGIS](https://qgis.org/download/)（免费开源）
2. 打开 QGIS
3. 把 `mosaic.vrt` 这个文件**直接拖进 QGIS 窗口**
4. 你会看到所有地图按正确位置拼在一起

---

## 出错了怎么办

### ① 双击 .bat 一闪而过 / 提示"没有找到 Python"

Python 没装，或者装的时候**没勾 `Add Python to PATH`**。

解决：重新运行 Python 安装程序，选 **Modify**，
确保勾上 `Add Python to PATH`。或者卸载重装，安装第一屏记得勾。

### ② 提示"缺少依赖库"然后安装失败

多半是网络问题。在文件夹空白处 `Shift + 右键` → **在此处打开 PowerShell**，
手动执行：

```
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

（后面那段是清华镜像，国内下载快很多）

### ③ 发现某幅图的**地图内容被切掉了一块** ⚠

这是最需要重视的问题。用记事本打开 `config.yaml`，找到 `force_safe`：

```yaml
crop:
  force_safe: ["0217-桐山"]
```

把被切的那幅的**文件名（不含扩展名）**加进去，像这样：

```yaml
crop:
  force_safe: ["0217-桐山", "1506-湖陽鎮"]
```

重新运行，这一幅就会走"整幅保留"路径——**边缘可能留白，但绝不会切内容**。

> 顺便把出问题的文件名记下来反馈给技术人员，这些样本很宝贵。

### ④ 大部分图都留白很多

用记事本打开 `config.yaml`，找到 `density_threshold: 0.10`，
把它改成 `0.12` 或 `0.13`，然后**先只放一幅图试跑**看效果。

如果反而开始切内容了，就改回 `0.10` 或更低（`0.08`）。

### ⑤ 提示"文件名不符合规则，已跳过 xxx"

文件名格式不对。检查是不是**四位数字开头、后面跟一个短横线**：

- ✗ `瑪瑙觀.jpg`（没有数字）
- ✗ `2108瑪瑙觀.jpg`（数字后面少了横线）
- ✗ `21-08-瑪瑙觀.jpg`（列和行分开了）
- ✓ `2108-瑪瑙觀.jpg`

### ⑥ 图在 GIS 里整体偏了

老地图和现代地图之间常有系统性偏移。用记事本打开 `config.yaml`，
找到 `east_shift_m: 0.0`，把它改成偏移的米数（向东为正），比如 `east_shift_m: 6000.0`。

> ⚠ 这个值换算成经度时依赖纬度。如果同时移动了图幅的南北位置，
> 需要重新计算，请交给技术人员处理。

---

## 名词速查

| 词 | 意思 |
|---|---|
| **图廓** | 地图四周的边框线，框内才是地图内容 |
| **图幅** | 一幅地图，也就是一个扫描文件 |
| **裁切** | 把图廓外面的黑边、空白、标题都裁掉 |
| **配准** | 告诉电脑"这张图应该在地球的这个位置" |
| **经纬度** | 表示地球位置的坐标，比如东经 111°、北纬 32° |
| **GIS** | 地理信息系统，能在地图上叠加数据的软件，如 QGIS、ArcGIS |
| **GeoTIFF** | 带位置信息的图片文件，GIS 能自动认到正确位置 |
| **VRT** | 一个"索引文件"，本身不是图片，但能指向一堆图片并拼在一起 |
| **接图表** | 一张总图，上面画着所有图幅的格子，标着每格的图号 |
| **金字塔** | 图片的多个缩小版本，让 GIS 缩放时更快 |

---

## 文件都是干什么的

```
historical-map-toolkit\
├── 一键运行.bat          ← 双击这个就跑（最常用）
├── 生成示例数据.bat       ← 想先试效果时双击这个
├── 新手从这里开始.md      ← 你正在看的文件
├── README.md             更详细的技术说明
├── config.yaml           配置文件，用记事本打开改
├── input\                ← 把你的地图放这里
├── work\                 ← 跑完的成果在这里
├── docs\                 原理和参数说明（进阶阅读）
└── scripts\              程序本体，不用动
```

**你平时只会用到两个 .bat 文件和 `input` 文件夹，其他的不用管。**
