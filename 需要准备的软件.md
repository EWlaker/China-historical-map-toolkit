# 需要准备哪些软件

**结论先说：只必装 1 个（Python）。** 其余都是可选，
不装也能把图裁好，只是看不到"带坐标"和"整体拼接"的效果。

---

## 一、必装：Python

这是唯一的硬性要求。

| 项 | 说明 |
|---|---|
| 版本 | **3.9 或更新**（实测 3.12 可用；建议 3.10~3.12） |
| 费用 | 免费 |
| 下载 | <https://www.python.org/downloads/> |
| 安装注意事项 | ⚠ **安装第一屏最下方务必勾选 `Add Python to PATH`** |

> **不勾那个框会怎样？**
> 后面双击 `一键运行.bat` 会提示"没有找到 Python"。
> 补救办法：重新运行安装程序 → 选 **Modify** → 确认勾上 `Add Python to PATH`；
> 或者卸载后重装。

### 依赖库不用你管

工具需要 5 个 Python 库，`一键运行.bat` 会在第一次运行时**自动安装**：

| 库 | 作用 |
|---|---|
| numpy | 数组运算 |
| opencv-python | 图像处理（纠斜、找图廓、裁切） |
| Pillow | 读写图片（兼容中文路径） |
| **rasterio** | **写带坐标的 GeoTIFF。它自带 GDAL，所以不需要另外装 GDAL** |
| PyYAML | 读配置文件 |

安装需要联网，大约 1~3 分钟。装一次以后就不再需要了。

---

## 二、建议装：QGIS

**只有你想看"带坐标的成果"和"整幅拼接效果"时才需要。**

| 项 | 说明 |
|---|---|
| 版本 | 3.28 或更新（任意新版都行） |
| 费用 | **免费开源** |
| 下载 | <https://qgis.org/download/> |
| 用途 | 打开 `work\02_geo\mosaic.vrt` 查看拼接结果；查看图幅位置是否正确 |

**不装 QGIS 有什么影响？**
`work\01_crop\` 里的裁切成果是**普通 PNG 图片**，双击就能看，
完全不需要 QGIS。只有带坐标的 `work\02_geo\` 才需要 GIS 软件打开。

---

## 三、可选（系统一般已有）

| 软件 | 说明 |
|---|---|
| 解压工具 | Windows 10/11 右键自带"全部解压缩"；习惯用 7-Zip / Bandizip 也行 |
| 图片查看器 | 系统自带"照片"就够 |
| 文本编辑器 | 改配置用**记事本**即可；推荐 VS Code / Notepad++（能正确显示 UTF-8） |

> 用记事本改 `config.yaml` 时注意：**另存为时编码选 UTF-8**，
> 否则中文图名会乱码。

---

## 四、不需要装（别被误导）

这几样经常被误以为要装，其实都不需要：

| 软件 | 为什么不需要 |
|---|---|
| **GDAL**（单独安装） | rasterio 已把 GDAL 打包进去，独立安装反而容易版本冲突 |
| **ArcGIS / ArcGIS Pro** | 收费商业软件；本工具输出的是标准 GeoTIFF，如果你单位有 ArcGIS，装不装都能用本工具 |
| **Anaconda / Miniconda** | 不需要。用官网的官方 Python 即可，环境更简单 |
| **Photoshop** | 本工具不做图像美化，不涉及 |
| **Office / WPS** | 不涉及 |
| **Node.js / Java** | 不涉及 |

---

## 五、硬件与磁盘空间

| 项 | 建议 | 说明 |
|---|---|---|
| 内存 | **8 GB 以上** | 单幅大扫描件（约 6400×5500）解码后占约 100 MB，叠加处理中间量后峰值数百 MB |
| 磁盘 | 预留 **输入数据量的 2.5 倍** | 见下面估算 |
| CPU | 普通即可 | 脚本目前是单进程，核心多帮助有限 |

**磁盘空间估算（以 400 幅、每幅 15 MB 为例）：**

| 内容 | 占用 |
|---|---|
| 原始扫描件（input） | 约 6.0 GB |
| 裁切成果（work\01_crop） | 约 2.0 GB |
| 配准成果（work\02_geo） | 约 6.5 GB |
| **合计** | **约 15 GB** |

> 如果磁盘紧张，`work\01_crop\` 在配准完成后可以删掉——
> 它只是中间产物，`work\02_geo\` 已经是最终成果。

---

## 六、安装顺序

```
1. 装 Python            （勾 Add Python to PATH）
        ↓
2. 解压工具包到无中文路径的目录，如 D:\map-toolkit\
        ↓
3. 双击 一键运行.bat     （第一次会自动装那 5 个依赖库）
        ↓
4. 装 QGIS（可选）       想在 GIS 里看整体时再装，随时都行
```

---

## 七、装完怎么确认没问题

打开 `cmd`（按 `Win + R`，输入 `cmd` 回车），依次输入：

```cmd
python --version
```

应该显示 `Python 3.x.x`。如果提示"不是内部或外部命令"，就是 **PATH 没配好**，
回到第一节重装。

```cmd
python -c "import cv2, numpy, rasterio, yaml, PIL; print('deps OK')"
```

显示 `deps OK` 就说明 5 个库都在了。
如果报 `ModuleNotFoundError`，说明依赖没装成功，
在工具包文件夹里双击 `一键运行.bat` 让它重装一次。

> 这条命令故意用英文输出（`deps OK`）：中文在 cmd 里受代码页影响，
> 换个系统可能显示成乱码，英文则到哪都一样。

---

## 八、一页速查

| 软件 | 必需？ | 免费？ | 不装的后果 |
|---|---|---|---|
| **Python 3.9+** | ✅ **必需** | 免费 | 完全没法用 |
| QGIS | ⭕ 建议 | 免费 | 只看不了带坐标的成果和整体拼接 |
| 解压工具 | ⭕ 可选 | — | Win10/11 自带即可 |
| 记事本 | ⭕ 可选 | 自带 | 用别的编辑器也行 |
| GDAL | ❌ 不需要 | — | — |
| ArcGIS | ❌ 不需要 | — | — |
| Anaconda | ❌ 不需要 | — | — |
