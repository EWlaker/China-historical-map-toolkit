# 历史地形图批量裁切与配准工具包 — 依赖
# 安装:  pip install -r requirements.txt
#
# 说明: rasterio 是 PyPI 上自带 GDAL 二进制的发行版, 不需要另外安装 GDAL,
#       这是本工具包能做到"解压即用"的关键。

numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
rasterio>=1.3
PyYAML>=6.0

# --- 可选: 只有 scripts/tools/build_index_sheets.py 需要 ---
# geopandas>=0.14
# shapely>=2.0
