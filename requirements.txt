# Historical Map Toolkit - Python dependencies
# Install:  pip install -r requirements.txt
#
# ---------------------------------------------------------------------------
# NOTE: This file is intentionally ASCII-only. Do not add non-ASCII characters.
#
# pip reads requirements files using the system locale encoding on some
# versions. On a Chinese Windows (cp936/GBK locale), UTF-8 Chinese comments in
# this file cause:
#     UnicodeDecodeError: 'gbk' codec can't decode byte ...
# and the install fails. Keeping it ASCII-only avoids the problem entirely.
# ---------------------------------------------------------------------------
#
# rasterio ships its own GDAL binaries on PyPI, so GDAL does NOT need to be
# installed separately. This is what makes the toolkit work out of the box.

numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
rasterio>=1.3
PyYAML>=6.0

# --- Optional: only needed by scripts/tools/build_index_sheets.py ---
# geopandas>=0.14
# shapely>=2.0
